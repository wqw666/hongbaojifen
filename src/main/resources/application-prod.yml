# 生产环境配置
# 启动: java -jar hongbaojifen-api.jar --spring.profiles.active=prod
server:
  port: 8892

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/hongbaojifen?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai&sessionVariables=sql_mode='ANSI_QUOTES'
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password: 19991020

app:
  jwt:
    secret: hbjf_prod_secret_2026_change_me
    expire-hours: 24
  open:
    api-key: hbjf-open-2026
  rules-dir: /opt/hongbaojifen/data/rules
